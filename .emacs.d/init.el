;; Use left Option as Meta, while keeping right Option available for
;; normal macOS character entry (for example, accented characters).
(setq mac-option-modifier 'meta
      mac-right-option-modifier nil)

(setq mac-command-modifier 'super)
